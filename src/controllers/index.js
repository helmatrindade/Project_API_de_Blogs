const loginController = require('./loginController');
const userController = require('./userController');
const categoryService = require('./categoryController');

module.exports = {
  loginController,
  userController,
  categoryService,
};
